pandas
openpyxl
pyautogui
pyperclip
anki
# pymupdf  # Temporarily disabled on the fast-build branch.
# Pillow  # Temporarily disabled on the fast-build branch.
# paddlepaddle>=3.0  # Temporarily disabled on the fast-build branch.
# paddleocr  # Temporarily disabled on the fast-build branch.
